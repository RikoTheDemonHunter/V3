local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Lighting = game:GetService("Lighting")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local SETTINGS_FILE = "AveryHubConfig.json"
local SavedSettings = {}

local function LoadSettings()
	if isfile and readfile and isfile(SETTINGS_FILE) then
		local ok, decoded = pcall(function()
			return HttpService:JSONDecode(readfile(SETTINGS_FILE))
		end)
		if ok and type(decoded) == "table" then
			SavedSettings = decoded
		end
	end
end

local function SaveSettings()
	if writefile then
		local ok, encoded = pcall(function()
			return HttpService:JSONEncode(SavedSettings)
		end)
		if ok then
			writefile(SETTINGS_FILE, encoded)
		end
	end
end

LoadSettings()

local Theme = {
	Background = Color3.fromRGB(12, 14, 22),
	TopBar = Color3.fromRGB(16, 18, 28),
	Sidebar = Color3.fromRGB(18, 20, 31),
	Card = Color3.fromRGB(24, 27, 41),
	CardHover = Color3.fromRGB(30, 34, 50),
	Accent = Color3.fromRGB(0, 200, 255),
	Text = Color3.fromRGB(245, 245, 245),
	Muted = Color3.fromRGB(170, 175, 190),
	Danger = Color3.fromRGB(255, 85, 85),
	Success = Color3.fromRGB(80, 220, 140),
	Highlight = Color3.fromRGB(255, 215, 0)
}

local function makeCorner(parent, radius)
	local c = Instance.new("UICorner")
	c.CornerRadius = UDim.new(0, radius or 8)
	c.Parent = parent
	return c
end

local function makeStroke(parent, color, thickness, transparency)
	local s = Instance.new("UIStroke")
	s.Color = color or Theme.Accent
	s.Thickness = thickness or 1
	s.Transparency = transparency or 0
	s.Parent = parent
	return s
end

local function tween(obj, time, props)
	local t = TweenService:Create(obj, TweenInfo.new(time or 0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), props)
	t:Play()
	return t
end

local function fitCanvas(scroller, list)
	list:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
		scroller.CanvasSize = UDim2.new(0, 0, 0, list.AbsoluteContentSize.Y + 18)
	end)
end

local ModernLib = {}

function ModernLib:CreateMain(hubTitle)
	local ScreenGui = Instance.new("ScreenGui")
	ScreenGui.Name = "AveryHubUI"
	ScreenGui.ResetOnSpawn = false
	ScreenGui.IgnoreGuiInset = true
	ScreenGui.Parent = game.CoreGui

	local MainFrame = Instance.new("Frame")
	MainFrame.Name = "Main"
	MainFrame.Size = UDim2.new(0, 720, 0, 460)
	MainFrame.Position = UDim2.new(0.5, -360, 0.5, -230)
	MainFrame.BackgroundColor3 = Theme.Background
	MainFrame.BorderSizePixel = 0
	MainFrame.Parent = ScreenGui
	makeCorner(MainFrame, 16)
	makeStroke(MainFrame, Theme.Accent, 1.5, 0.25)

	local Shadow = Instance.new("ImageLabel")
	Shadow.BackgroundTransparency = 1
	Shadow.Image = "rbxassetid://1316045217"
	Shadow.ImageColor3 = Color3.new(0, 0, 0)
	Shadow.ImageTransparency = 0.4
	Shadow.ScaleType = Enum.ScaleType.Slice
	Shadow.SliceCenter = Rect.new(10, 10, 118, 118)
	Shadow.Size = UDim2.new(1, 36, 1, 36)
	Shadow.Position = UDim2.new(0, -18, 0, -18)
	Shadow.ZIndex = 0
	Shadow.Parent = MainFrame

	local TopBar = Instance.new("Frame")
	TopBar.Size = UDim2.new(1, 0, 0, 52)
	TopBar.BackgroundColor3 = Theme.TopBar
	TopBar.BorderSizePixel = 0
	TopBar.Parent = MainFrame
	makeCorner(TopBar, 16)

	local TopCover = Instance.new("Frame")
	TopCover.Size = UDim2.new(1, 0, 0, 16)
	TopCover.Position = UDim2.new(0, 0, 1, -16)
	TopCover.BackgroundColor3 = Theme.TopBar
	TopCover.BorderSizePixel = 0
	TopCover.Parent = TopBar

	local TitleLabel = Instance.new("TextLabel")
	TitleLabel.Size = UDim2.new(1, -130, 1, 0)
	TitleLabel.Position = UDim2.new(0, 18, 0, 0)
	TitleLabel.BackgroundTransparency = 1
	TitleLabel.Font = Enum.Font.GothamBold
	TitleLabel.Text = hubTitle
	TitleLabel.TextColor3 = Theme.Text
	TitleLabel.TextSize = 19
	TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
	TitleLabel.Parent = TopBar

	local CloseBtn = Instance.new("TextButton")
	CloseBtn.Size = UDim2.new(0, 30, 0, 30)
	CloseBtn.Position = UDim2.new(1, -40, 0.5, -15)
	CloseBtn.BackgroundColor3 = Theme.Danger
	CloseBtn.Text = "×"
	CloseBtn.TextColor3 = Color3.new(1, 1, 1)
	CloseBtn.Font = Enum.Font.GothamBold
	CloseBtn.TextSize = 18
	CloseBtn.BorderSizePixel = 0
	CloseBtn.Parent = TopBar
	makeCorner(CloseBtn, 8)

	local MinimizeBtn = Instance.new("TextButton")
	MinimizeBtn.Size = UDim2.new(0, 30, 0, 30)
	MinimizeBtn.Position = UDim2.new(1, -76, 0.5, -15)
	MinimizeBtn.BackgroundColor3 = Theme.Card
	MinimizeBtn.Text = "—"
	MinimizeBtn.TextColor3 = Theme.Text
	MinimizeBtn.Font = Enum.Font.GothamBold
	MinimizeBtn.TextSize = 18
	MinimizeBtn.BorderSizePixel = 0
	MinimizeBtn.Parent = TopBar
	makeCorner(MinimizeBtn, 8)

	local Body = Instance.new("Frame")
	Body.Size = UDim2.new(1, 0, 1, -52)
	Body.Position = UDim2.new(0, 0, 0, 52)
	Body.BackgroundTransparency = 1
	Body.Parent = MainFrame

	local Sidebar = Instance.new("ScrollingFrame")
	Sidebar.Size = UDim2.new(0, 180, 1, -20)
	Sidebar.Position = UDim2.new(0, 12, 0, 10)
	Sidebar.BackgroundColor3 = Theme.Sidebar
	Sidebar.BorderSizePixel = 0
	Sidebar.ScrollBarThickness = 3
	Sidebar.ScrollBarImageColor3 = Theme.Accent
	Sidebar.CanvasSize = UDim2.new(0, 0, 0, 0)
	Sidebar.Parent = Body
	makeCorner(Sidebar, 12)

	local sidePad = Instance.new("UIPadding")
	sidePad.PaddingTop = UDim.new(0, 12)
	sidePad.PaddingBottom = UDim.new(0, 12)
	sidePad.PaddingLeft = UDim.new(0, 12)
	sidePad.PaddingRight = UDim.new(0, 12)
	sidePad.Parent = Sidebar

	local SideList = Instance.new("UIListLayout")
	SideList.Padding = UDim.new(0, 8)
	SideList.SortOrder = Enum.SortOrder.LayoutOrder
	SideList.Parent = Sidebar
	fitCanvas(Sidebar, SideList)

	local Container = Instance.new("Frame")
	Container.Size = UDim2.new(1, -208, 1, -20)
	Container.Position = UDim2.new(0, 200, 0, 10)
	Container.BackgroundTransparency = 1
	Container.Parent = Body

	local accentObjects = {}
	local activePage
	local activeTabButton
	local activeConnection

	local function registerAccent(obj, prop)
		table.insert(accentObjects, {Obj = obj, Prop = prop})
		obj[prop] = Theme.Accent
	end

	local function applyAccent(color)
		Theme.Accent = color
		for _, item in ipairs(accentObjects) do
			if item.Obj and item.Obj.Parent then
				item.Obj[item.Prop] = color
			end
		end
	end

	local function StartCycleAccent(mode)
		if activeConnection then
			activeConnection:Disconnect()
			activeConnection = nil
		end

		activeConnection = RunService.RenderStepped:Connect(function()
			local color
			if mode == "Rainbow" then
				color = Color3.fromHSV((tick() % 4) / 4, 1, 1)
			elseif mode == "Ethereal" then
				local s = (math.sin(tick() * 1.2) + 1) / 2
				color = Color3.fromRGB(0, 170, 255):Lerp(Color3.fromRGB(255, 100, 200), s)
			end

			if color then
				Theme.Accent = color
				for _, item in ipairs(accentObjects) do
					if item.Obj and item.Obj.Parent then
						item.Obj[item.Prop] = color
					end
				end
			end
		end)
	end

	local Tabs = { ActivePage = nil }

	function Tabs:Tab(tabName)
		local TabBtn = Instance.new("TextButton")
		TabBtn.Size = UDim2.new(1, 0, 0, 38)
		TabBtn.BackgroundColor3 = Theme.Card
		TabBtn.Text = tabName
		TabBtn.TextColor3 = Theme.Muted
		TabBtn.Font = Enum.Font.GothamSemibold
		TabBtn.TextSize = 13
		TabBtn.BorderSizePixel = 0
		TabBtn.AutoButtonColor = false
		TabBtn.Parent = Sidebar
		makeCorner(TabBtn, 10)
		makeStroke(TabBtn, Theme.Accent, 1, 0.9)

		local TabAccent = Instance.new("Frame")
		TabAccent.Size = UDim2.new(0, 4, 1, -14)
		TabAccent.Position = UDim2.new(0, 6, 0, 7)
		TabAccent.BackgroundColor3 = Theme.Accent
		TabAccent.BorderSizePixel = 0
		TabAccent.Visible = false
		TabAccent.Parent = TabBtn
		makeCorner(TabAccent, 99)
		registerAccent(TabAccent, "BackgroundColor3")

		local Page = Instance.new("ScrollingFrame")
		Page.Size = UDim2.new(1, 0, 1, 0)
		Page.BackgroundTransparency = 1
		Page.ScrollBarThickness = 3
		Page.ScrollBarImageColor3 = Theme.Accent
		Page.Visible = false
		Page.CanvasSize = UDim2.new(0, 0, 0, 0)
		Page.Parent = Container

		local pagePad = Instance.new("UIPadding")
		pagePad.PaddingTop = UDim.new(0, 6)
		pagePad.PaddingLeft = UDim.new(0, 4)
		pagePad.PaddingRight = UDim.new(0, 4)
		pagePad.Parent = Page

		local PageList = Instance.new("UIListLayout")
		PageList.Padding = UDim.new(0, 10)
		PageList.SortOrder = Enum.SortOrder.LayoutOrder
		PageList.Parent = Page
		fitCanvas(Page, PageList)

		local function setActive()
			if activePage then activePage.Visible = false end
			if activeTabButton then
				activeTabButton.BackgroundColor3 = Theme.Card
				activeTabButton.TextColor3 = Theme.Muted
				local prev = activeTabButton:FindFirstChild("Accent")
				if prev then prev.Visible = false end
			end

			activePage = Page
			activeTabButton = TabBtn
			Page.Visible = true
			TabBtn.BackgroundColor3 = Color3.fromRGB(32, 37, 54)
			TabBtn.TextColor3 = Color3.new(1, 1, 1)
			TabAccent.Visible = true
			Tabs.ActivePage = Page
		end

		TabBtn.MouseEnter:Connect(function()
			if activeTabButton ~= TabBtn then
				tween(TabBtn, 0.15, {BackgroundColor3 = Theme.CardHover})
			end
		end)

		TabBtn.MouseLeave:Connect(function()
			if activeTabButton ~= TabBtn then
				tween(TabBtn, 0.15, {BackgroundColor3 = Theme.Card})
			end
		end)

		TabBtn.MouseButton1Click:Connect(setActive)

		if not Tabs.ActivePage then
			setActive()
		end

		local Elements = {}

		function Elements:Section(textString)
			local Section = Instance.new("TextLabel")
			Section.Size = UDim2.new(1, 0, 0, 22)
			Section.BackgroundTransparency = 1
			Section.Text = textString
			Section.TextColor3 = Theme.Highlight
			Section.Font = Enum.Font.GothamBold
			Section.TextSize = 13
			Section.TextXAlignment = Enum.TextXAlignment.Left
			Section.Parent = Page
			return Section
		end

		function Elements:Label(textString)
			local Lbl = Instance.new("TextLabel")
			Lbl.Size = UDim2.new(1, 0, 0, 24)
			Lbl.BackgroundTransparency = 1
			Lbl.Text = textString
			Lbl.TextColor3 = Theme.Text
			Lbl.Font = Enum.Font.Gotham
			Lbl.TextSize = 13
			Lbl.TextXAlignment = Enum.TextXAlignment.Left
			Lbl.Parent = Page
			return Lbl
		end

		function Elements:Button(btnName, callback)
			local Btn = Instance.new("TextButton")
			Btn.Size = UDim2.new(1, 0, 0, 40)
			Btn.BackgroundColor3 = Theme.Card
			Btn.Text = btnName
			Btn.TextColor3 = Theme.Accent
			Btn.Font = Enum.Font.GothamSemibold
			Btn.TextSize = 14
			Btn.BorderSizePixel = 0
			Btn.AutoButtonColor = false
			Btn.Parent = Page
			makeCorner(Btn, 10)
			makeStroke(Btn, Theme.Accent, 1, 0.85)

			Btn.MouseEnter:Connect(function()
				tween(Btn, 0.15, {BackgroundColor3 = Theme.CardHover})
			end)

			Btn.MouseLeave:Connect(function()
				tween(Btn, 0.15, {BackgroundColor3 = Theme.Card})
			end)

			Btn.MouseButton1Click:Connect(function()
				tween(Btn, 0.08, {Size = UDim2.new(1, -2, 0, 38)})
				task.wait()
				tween(Btn, 0.08, {Size = UDim2.new(1, 0, 0, 40)})
				if callback then callback() end
			end)

			registerAccent(Btn, "TextColor3")
		end

		function Elements:Toggle(toggleName, callback)
			local ToggleFrame = Instance.new("Frame")
			ToggleFrame.Size = UDim2.new(1, 0, 0, 44)
			ToggleFrame.BackgroundColor3 = Theme.Card
			ToggleFrame.BorderSizePixel = 0
			ToggleFrame.Parent = Page
			makeCorner(ToggleFrame, 10)
			makeStroke(ToggleFrame, Theme.Accent, 1, 0.9)

			local lbl = Instance.new("TextLabel")
			lbl.Size = UDim2.new(1, -90, 1, 0)
			lbl.Position = UDim2.new(0, 12, 0, 0)
			lbl.BackgroundTransparency = 1
			lbl.Text = toggleName
			lbl.TextColor3 = Theme.Text
			lbl.Font = Enum.Font.Gotham
			lbl.TextSize = 13
			lbl.TextXAlignment = Enum.TextXAlignment.Left
			lbl.Parent = ToggleFrame

			local Switch = Instance.new("TextButton")
			Switch.Size = UDim2.new(0, 56, 0, 24)
			Switch.Position = UDim2.new(1, -68, 0.5, -12)
			Switch.BackgroundColor3 = Theme.Danger
			Switch.Text = "OFF"
			Switch.TextColor3 = Color3.new(1, 1, 1)
			Switch.Font = Enum.Font.GothamBold
			Switch.TextSize = 11
			Switch.BorderSizePixel = 0
			Switch.AutoButtonColor = false
			Switch.Parent = ToggleFrame
			makeCorner(Switch, 12)

			local knob = Instance.new("Frame")
			knob.Size = UDim2.new(0, 18, 0, 18)
			knob.Position = UDim2.new(0, 3, 0.5, -9)
			knob.BackgroundColor3 = Color3.new(1, 1, 1)
			knob.BorderSizePixel = 0
			knob.Parent = Switch
			makeCorner(knob, 9)

			local state = SavedSettings[toggleName] == true

			local function setState(v)
				state = v
				Switch.Text = v and "ON" or "OFF"
				Switch.BackgroundColor3 = v and Theme.Success or Theme.Danger
				tween(knob, 0.12, {Position = v and UDim2.new(1, -21, 0.5, -9) or UDim2.new(0, 3, 0.5, -9)})
				SavedSettings[toggleName] = v
				SaveSettings()
				if callback then callback(v) end
			end

			Switch.MouseEnter:Connect(function()
				tween(Switch, 0.12, {BackgroundTransparency = 0.1})
			end)
			Switch.MouseLeave:Connect(function()
				tween(Switch, 0.12, {BackgroundTransparency = 0})
			end)

			Switch.MouseButton1Click:Connect(function()
				setState(not state)
			end)

			setState(state)
		end

		return Elements
	end

	function Tabs:SetTheme(themeName)
		SavedSettings.ActiveTheme = themeName
		SaveSettings()

		local themes = {
			["Ethereal Blue"] = Color3.fromRGB(0, 200, 255),
			["Light Blue"] = Color3.fromRGB(130, 215, 255),
			["Lime"] = Color3.fromRGB(110, 240, 95),
			["Pink"] = Color3.fromRGB(255, 110, 200),
			["Red"] = Color3.fromRGB(255, 75, 75),
			["Orange"] = Color3.fromRGB(255, 140, 50)
		}

		if themeName == "Rainbow" then
			StartCycleAccent("Rainbow")
		elseif themeName == "Ethereal Cycle" then
			StartCycleAccent("Ethereal")
		else
			if activeConnection then
				activeConnection:Disconnect()
				activeConnection = nil
			end
			applyAccent(themes[themeName] or Theme.Accent)
		end
	end

	function Tabs:SetStyle(styleName)
		SavedSettings.ActiveStyle = styleName
		SaveSettings()

		if styleName == "Transparent" then
			MainFrame.BackgroundTransparency = 0.25
			TopBar.BackgroundTransparency = 0.15
			Sidebar.BackgroundTransparency = 0.25
		elseif styleName == "Glossy" then
			MainFrame.BackgroundTransparency = 0.1
			MainFrame.BackgroundColor3 = Color3.fromRGB(22, 25, 38)
			TopBar.BackgroundColor3 = Color3.fromRGB(26, 30, 45)
			Sidebar.BackgroundColor3 = Color3.fromRGB(20, 23, 35)
		else
			MainFrame.BackgroundTransparency = 0
			TopBar.BackgroundTransparency = 0
			Sidebar.BackgroundTransparency = 0
			MainFrame.BackgroundColor3 = Theme.Background
			TopBar.BackgroundColor3 = Theme.TopBar
			Sidebar.BackgroundColor3 = Theme.Sidebar
		end
	end

	local dragging, dragStart, startPos
	TopBar.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = MainFrame.Position
			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then dragging = false end
			end)
		end
	end)

	UserInputService.InputChanged:Connect(function(input)
		if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
			local delta = input.Position - dragStart
			tween(MainFrame, 0.08, {
				Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
			})
		end
	end)

	CloseBtn.MouseButton1Click:Connect(function()
		ScreenGui:Destroy()
	end)

	MinimizeBtn.MouseButton1Click:Connect(function()
		MainFrame.Visible = not MainFrame.Visible
	end)

	task.spawn(function()
		local theme = SavedSettings.ActiveTheme or "Ethereal Blue"
		tabs:SetTheme(theme)
	end)

	task.spawn(function()
		local style = SavedSettings.ActiveStyle or "Default"
		tabs:SetStyle(style)
	end)

	return tabs
end

local Library = ModernLib:CreateMain("⚡ Avery Hub V3 | Premium Dashboard ⚡")

local AutoFarm = Library:Tab("AutoDrink")
local LocalPlayer = Library:Tab("LocalPlayer")
local Teleport = Library:Tab("Teleports")
local Themes = Library:Tab("Themes")
local Misc = Library:Tab("Misc")
local Scripts = Library:Tab("Scripts")
local Credits = Library:Tab("Credits")

Themes:Section("UI Accents")
Themes:Button("Ethereal Blue", function() Library:SetTheme("Ethereal Blue") end)
Themes:Button("Light Blue", function() Library:SetTheme("Light Blue") end)
Themes:Button("Lime", function() Library:SetTheme("Lime") end)
Themes:Button("Pink", function() Library:SetTheme("Pink") end)
Themes:Button("Red", function() Library:SetTheme("Red") end)
Themes:Button("Orange", function() Library:SetTheme("Orange") end)
Themes:Button("Ethereal Cycle", function() Library:SetTheme("Ethereal Cycle") end)
Themes:Button("Rainbow Cycle", function() Library:SetTheme("Rainbow") end)

Themes:Section("UI Style")
Themes:Button("Default", function() Library:SetStyle("Default") end)
Themes:Button("Transparent", function() Library:SetStyle("Transparent") end)
Themes:Button("Glossy", function() Library:SetStyle("Glossy") end)

Credits:Section("About")
Credits:Label("Developer: Avery")
Credits:Label("Build: Modernized UI")
Credits:Label("Discord: 90averyxx")
Credits:Label("Whitelist System")

local DateTimeLabel = Credits:Label("Date & Time: Loading...")
local LocationLabel = Credits:Label("Location: Loading...")

task.spawn(function()
	while task.wait(1) do
		if DateTimeLabel and DateTimeLabel.Parent then
			DateTimeLabel.Text = "Date & Time: " .. os.date("%A, %B %d %Y | %I:%M:%S %p")
		else
			break
		end
	end
end)

task.spawn(function()
	local ok, result = pcall(function()
		return game:HttpGet("http://ip-api.com/json/")
	end)
	if ok and result then
		local decodeOk, decoded = pcall(function()
			return HttpService:JSONDecode(result)
		end)
		if decodeOk and decoded and decoded.city and decoded.country then
			LocationLabel.Text = "Location: " .. decoded.city .. ", " .. decoded.country
		else
			LocationLabel.Text = "Location: Unknown"
		end
	else
		LocationLabel.Text = "Location: Unknown"
	end
end)
